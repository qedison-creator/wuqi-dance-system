/* iPhone 风格日期选择器 - 底部抽屉式 */
.dp-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 9999;
  display: flex;
  align-items: flex-end;
}

.dp-sheet {
  width: 100%;
  background: #FFFFFF;
  border-radius: 24rpx 24rpx 0 0;
  padding-bottom: env(safe-area-inset-bottom);
  animation: dpSlideUp 240ms cubic-bezier(0.32, 0.72, 0, 1);
}

@keyframes dpSlideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.dp-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  border-bottom: 1rpx solid #EFEAE0;
}

.dp-cancel {
  font-size: 28rpx;
  color: #8B7E6A;
  padding: 8rpx 12rpx;
}

.dp-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #2C2416;
}

.dp-confirm {
  font-size: 28rpx;
  color: #C4663E;
  font-weight: 600;
  padding: 8rpx 12rpx;
}

.dp-body {
  position: relative;
  padding: 0 32rpx;
}

.dp-indicator {
  position: absolute;
  left: 32rpx;
  right: 32rpx;
  top: 50%;
  transform: translateY(-50%);
  height: 96rpx;
  background: #F6F3F0;
  border-radius: 12rpx;
  pointer-events: none;
  z-index: 0;
}

.dp-picker {
  position: relative;
  z-index: 1;
  height: 384rpx;
}

.dp-col-item {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: #2C2416;
  height: 96rpx;
  line-height: 96rpx;
}
