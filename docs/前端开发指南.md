# 舞栖舞蹈社 - 前端开发指南

## 📋 项目概述

舞栖舞蹈社包含两个小程序端：
- **会员端** - 会员预约、查看课程、个人中心等
- **管理端** - 门店管理、会员管理、排课、签到等

## 🏗 项目结构

```
wuqi-dance/
├── wuqi-member/       # 会员端小程序
│   ├── pages/
│   ├── components/
│   ├── custom-tab-bar/
│   ├── utils/
│   └── app.js
├── wuqi-admin/        # 管理端小程序
│   ├── pages/
│   ├── components/
│   ├── custom-tab-bar/
│   ├── utils/
│   └── app.js
└── wuqi-backend/      # 后端服务（已完成）
```

## 🔧 开发环境准备

### 1. 开发工具
- 微信开发者工具
- VS Code（推荐）

### 2. 后端服务
确保后端服务已启动并正常运行：
```bash
cd wuqi-backend
npm install
npm run seed      # 初始化数据
npm run dev       # 启动服务
```

服务地址：`http://localhost:3000`

## 📡 API 配置

### 1. 请求封装

在 `utils/request.js` 中配置 API 基础地址：

```javascript
const BASE_URL = 'http://localhost:3000/api/v1';

function request(url, method = 'GET', data = {}) {
  const token = wx.getStorageSync('token');
  
  return new Promise((resolve, reject) => {
    wx.request({
      url: BASE_URL + url,
      method,
      data,
      header: {
        'Content-Type': 'application/json',
        'Authorization': token ? `Bearer ${token}` : ''
      },
      success: (res) => {
        if (res.statusCode === 200) {
          if (res.data.code === 200) {
            resolve(res.data.data);
          } else {
            wx.showToast({
              title: res.data.message || '请求失败',
              icon: 'none'
            });
            reject(res.data);
          }
        } else if (res.statusCode === 401) {
          wx.removeStorageSync('token');
          wx.removeStorageSync('userInfo');
          wx.reLaunch({ url: '/pages/login/login' });
          reject(res);
        } else {
          wx.showToast({
            title: '网络错误',
            icon: 'none'
          });
          reject(res);
        }
      },
      fail: (err) => {
        wx.showToast({
          title: '网络错误',
          icon: 'none'
        });
        reject(err);
      }
    });
  });
}

module.exports = {
  get: (url, data) => request(url, 'GET', data),
  post: (url, data) => request(url, 'POST', data),
  put: (url, data) => request(url, 'PUT', data),
  delete: (url, data) => request(url, 'DELETE', data)
};
```

### 2. 认证状态管理

在 `app.js` 中管理登录状态：

```javascript
App({
  globalData: {
    userInfo: null,
    token: null,
    storeId: null
  },

  onLaunch() {
    const token = wx.getStorageSync('token');
    const userInfo = wx.getStorageSync('userInfo');
    
    if (token && userInfo) {
      this.globalData.token = token;
      this.globalData.userInfo = userInfo;
      this.globalData.storeId = userInfo.store_id;
    }
  },

  setUserInfo(userInfo) {
    this.globalData.userInfo = userInfo;
    wx.setStorageSync('userInfo', userInfo);
  },

  setToken(token) {
    this.globalData.token = token;
    wx.setStorageSync('token', token);
  },

  logout() {
    this.globalData.token = null;
    this.globalData.userInfo = null;
    wx.removeStorageSync('token');
    wx.removeStorageSync('userInfo');
  }
});
```

## 🎯 会员端功能开发

### 1. 首页

**功能点：**
- Banner 轮播
- 快捷入口
- 近期课程
- 我的预约

**API调用：**
```javascript
// 获取Banner列表
request.get('/banners')

// 获取课程列表
request.get('/schedules', { date: '2026-05-13' })

// 获取我的预约
request.get('/bookings/my', { type: 'upcoming' })
```

### 2. 课程预约

**功能点：**
- 课程列表（按日期筛选）
- 课程详情
- 预约课程
- 加入候补

**API调用：**
```javascript
// 获取排课列表
request.get('/schedules', { date, store_id })

// 获取排课详情
request.get(`/schedules/${scheduleId}`)

// 创建预约
request.post('/bookings', { schedule_id })

// 加入候补
request.post('/bookings/waitlist', { schedule_id })
```

**业务逻辑：**
1. 预约前检查会员信息是否完整
2. 检查套餐状态（未激活提示激活）
3. 检查课时是否充足

### 3. 我的课程

**功能点：**
- 已预约课程
- 已完成课程
- 取消预约
- 候补列表

**API调用：**
```javascript
// 获取我的预约
request.get('/bookings/my', { type: 'all', page, pageSize })

// 获取我的签到记录
request.get('/bookings/my-attendance')

// 获取我的候补列表
request.get('/bookings/waitlist/my')

// 取消预约
request.put(`/bookings/${bookingId}/cancel`)

// 候补确认预约
request.put(`/bookings/waitlist/confirm/${waitlistId}`)
```

### 4. 我的套餐

**功能点：**
- 套餐列表
- 当前套餐详情
- 激活套餐

**API调用：**
```javascript
// 获取我的套餐
request.get('/packages/my')

// 激活套餐
request.put('/packages/activate')

// 获取套餐状态
request.get(`/packages/member-status/${userId}`)
```

### 5. 个人中心

**功能点：**
- 个人信息展示
- 编辑个人信息
- 修改预留手机号（需审核）
- 会员二维码

**API调用：**
```javascript
// 获取个人信息
request.get('/auth/me')

// 更新个人信息
request.put('/members/profile/update', data)

// 申请修改预留手机号
request.post('/members/reserve-phone/request', { new_phone })

// 检查信息完整度
request.get(`/members/${userId}/info-status`)
```

**会员二维码生成：**
```javascript
// 会员编码格式：FY20260513001
// 使用 member_code 生成动态二维码
const memberCode = userInfo.member_code;
// 使用 wx.createQRCode 或第三方库生成
```

## 🏢 管理端功能开发

### 1. 登录页

**功能点：**
- 账号密码登录

**API调用：**
```javascript
request.post('/auth/admin-login', {
  username: 'admin',
  password: 'admin123'
})
```

### 2. 首页/仪表盘

**功能点：**
- 今日预约统计
- 今日签到统计
- 快捷操作入口

**API调用：**
```javascript
request.get('/stats/dashboard', { date })
```

### 3. 会员管理

**功能点：**
- 会员列表
- 会员详情
- 审核会员
- 录入套餐
- 设置豁免次数
- 分配会员编码
- 预留手机号审核
- 停卡/复卡

**API调用：**
```javascript
// 获取会员列表
request.get('/members', { keyword, status, store_id, page, pageSize })

// 获取会员详情
request.get(`/members/${memberId}`)

// 审核会员
request.put(`/members/${memberId}/review`, {
  action: 'approve|reject',
  reason,
  store_id
})

// 录入套餐
request.post('/packages', {
  user_id: memberId,
  package_id,
  package_type,
  total_credits,
  duration_value,
  duration_unit
})

// 设置豁免次数
request.put(`/members/${memberId}/exemption`, { exemption_count })

// 分配会员编码
request.put(`/members/${memberId}/assign-code`)

// 获取待审核手机号列表
request.get('/members/phone-audit/list')

// 审核预留手机号
request.put(`/members/${memberId}/phone-audit`, {
  action: 'approve|reject',
  reason
})

// 停卡
request.put(`/members/${memberId}/suspend`, { suspend_days })

// 复卡
request.put(`/members/${memberId}/unsuspend`)
```

### 4. 排课管理

**功能点：**
- 排课列表
- 创建排课
- 编辑排课
- 排课详情
- 查看候补名单

**API调用：**
```javascript
// 获取排课列表
request.get('/schedules', { date, store_id })

// 创建排课
request.post('/schedules', data)

// 更新排课
request.put(`/schedules/${scheduleId}`, data)

// 获取排课详情
request.get(`/schedules/${scheduleId}`)
```

### 5. 预约管理

**功能点：**
- 预约列表
- 会员签到（单个/批量）
- 查看签到记录
- 取消预约

**API调用：**
```javascript
// 获取预约列表
request.get('/bookings', { schedule_id, status })

// 单个会员签到
request.post('/bookings/check-in', {
  schedule_id,
  user_id
})

// 批量签到
request.post('/bookings/batch-check-in', {
  schedule_id,
  user_ids: [id1, id2, id3]
})

// 获取签到记录
request.get(`/bookings/check-in-records/${scheduleId}`)

// 管理员取消预约
request.put(`/bookings/${bookingId}/admin-cancel`, { reason })
```

### 6. 扫码签到

**功能点：**
- 扫描会员二维码
- 显示会员信息
- 一键签到

**实现思路：**
1. 使用 `wx.scanCode()` 扫描会员二维码
2. 解析二维码获取会员编码
3. 调用签到接口

```javascript
// 扫码签到
wx.scanCode({
  success: async (res) => {
    const memberCode = res.result; // 会员编码
    // 通过会员编码查找会员
    // 选择课程签到
    const { schedule_id, user_id } = data;
    await request.post('/bookings/check-in', {
      schedule_id,
      user_id
    });
    wx.showToast({ title: '签到成功' });
  }
});
```

### 7. 教练薪酬

**功能点：**
- 薪酬配置
- 薪酬统计
- 薪酬结算
- 数据导出

**API调用：**
```javascript
// 获取薪酬配置列表
request.get('/coach-salaries')

// 创建薪酬配置
request.post('/coach-salaries', {
  coach_id,
  store_id,
  duration,
  salary_rate,
  effective_from
})

// 获取薪酬统计列表
request.get('/coach-salaries/stats/list', {
  coach_id,
  start_date,
  end_date,
  status
})

// 获取薪酬汇总
request.get('/coach-salaries/stats/summary', {
  coach_id,
  start_date,
  end_date
})

// 结算薪酬
request.put(`/coach-salaries/stats/${statId}/settle`, { remark })
```

### 8. 放假管理

**功能点：**
- 放假设置
- 放假列表
- 撤销放假

**API调用：**
```javascript
request.get('/holidays')
request.post('/holidays', data)
request.put(`/holidays/${holidayId}`, data)
request.put(`/holidays/${holidayId}/revoke`)
```

## 🎨 UI/UX 建议

### 1. 设计规范
- 统一的颜色主题（建议主色调：紫色/粉色）
- 统一的字体大小和间距
- 统一的图标风格

### 2. 交互体验
- 加载状态提示
- 操作成功/失败反馈
- 表单验证提示
- 下拉刷新和上拉加载

### 3. 会员端特殊功能
- 动态二维码（自动刷新防截图）
- 预约前套餐状态检查弹窗
- 候补成功消息推送

### 4. 管理端特殊功能
- 扫码签到快速入口
- 批量操作支持
- 权限控制（按角色隐藏功能）

## 🔐 权限控制

### 管理端角色权限
```javascript
// 在 app.js 或全局工具中
function checkPermission(requiredRole) {
  const userInfo = getApp().globalData.userInfo;
  if (!userInfo) return false;
  
  const roleOrder = {
    'super_admin': 3,
    'store_manager': 2,
    'staff': 1
  };
  
  return roleOrder[userInfo.role] >= roleOrder[requiredRole];
}
```

## 📱 小程序配置

### 1. app.json 配置
```json
{
  "pages": [
    "pages/index/index",
    "pages/schedule/schedule",
    "pages/booking/booking",
    "pages/profile/profile",
    "pages/login/login"
  ],
  "window": {
    "navigationBarTitleText": "舞栖舞蹈社"
  },
  "tabBar": {
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页"
      },
      {
        "pagePath": "pages/schedule/schedule",
        "text": "课程"
      },
      {
        "pagePath": "pages/booking/booking",
        "text": "我的"
      }
    ]
  }
}
```

### 2. 权限配置
```json
{
  "permission": {
    "scope.userLocation": {
      "desc": "需要获取您的位置来推荐附近门店"
    },
    "scope.camera": {
      "desc": "需要使用相机进行扫码签到"
    }
  }
}
```

## 🧪 测试建议

### 1. 功能测试
- 会员注册登录流程
- 预约流程（完整/取消/候补）
- 签到流程
- 套餐激活/使用
- 管理端审核流程

### 2. 边界测试
- 课时不足时的预约
- 课程人数满员时的候补
- 放假期间的预约处理
- 权限不足的操作拦截

### 3. 兼容性测试
- iOS/Android 不同系统版本
- 不同屏幕尺寸适配

## 📚 相关文档

- [API文档](./API文档.md)
- [后端README](../wuqi-backend/README.md)
- [全面审查报告](./舞栖舞蹈社系统全面审查报告.md)

## 🚀 下一步

1. 启动后端服务
2. 初始化测试数据
3. 按照本指南开发前端功能
4. 进行完整测试
5. 部署上线

祝开发顺利！🎉
