/**
 * 数组包含判断
 * 用法：<view wx:if="{{cmp.contains(arr, val)}}"></view>
 */
var contains = function (arr, val) {
  if (!arr || !arr.length) return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) return true;
  }
  return false;
};

module.exports = {
  contains: contains
};
