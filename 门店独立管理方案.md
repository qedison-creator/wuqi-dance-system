# 门店独立管理方案（渐进式落地方案 v2）

## 一、方案定位

### 核心目标
实现「门店独立访问、独立管理」，每个门店的管理员登录后看到的是专属独立后台，感知不到其他门店的存在。

### 设计原则

1. **不改变现有 TabBar**：保持 首页/运营/会员/店务/我的 五个Tab不变
2. **不改变后端API路由和数据模型**：保持现有路由体系和User表
3. **页面内部自适应**：每个页面根据 `spaceMode`（平台模式/门店模式）渲染不同内容
4. **门店角色无感知**：门店角色登录后与现在体验完全一致（已有单门店隔离）
5. **超管增加"进入门店"能力**：可在平台视图和门店视图之间切换

---

## 二、总体架构

### 角色与空间映射

```
登录 → 后端返回 login_mode + 门店信息
         │
         ├─ 门店角色（staff / 单门店store_manager）
         │    spaceMode = 'store'（固定，不可切换）
         │    currentStoreId = 归属门店ID
         │    → 所有页面自动过滤本门店数据
         │    → 无门店选择器
         │    → 无平台功能入口
         │    → 导航栏无"返回平台"按钮
         │
         ├─ 超级管理员（super_admin）
         │    spaceMode = 'platform'（默认）
         │    currentStoreId = ''（空=全平台视图）
         │    → 首页显示全平台汇总 + 门店卡片列表
         │    → 点击门店卡片"进入门店"→ spaceMode切为'store'
         │    → 门店模式下导航栏显示"← 返回平台"
         │    → 门店模式下所有数据过滤到该门店
         │
         └─ 审核员（reviewer）
              spaceMode = 'platform'（固定）
              currentStoreId = ''
              → 全平台审核视图，只读
```

### 页面自适应逻辑

每个页面在 `onShow` 时读取全局状态，决定渲染内容：

```
┌──────────────────────────────────────────────┐
│  TabBar（不变）: [首页] [运营] [会员] [店务] [我的] │
├──────────────────────────────────────────────┤
│                                              │
│  每个页面内部判断 app.globalData.spaceMode：    │
│                                              │
│  spaceMode === 'platform'（超管默认）          │
│    → 首页：全平台汇总 + 门店卡片列表             │
│    → 运营：全平台运营数据                       │
│    → 会员：全平台会员列表                       │
│    → 店务：系统设置（门店管理/账号管理/全局配置）  │
│    → 我的：平台管理员信息                       │
│                                              │
│  spaceMode === 'store'（门店角色 / 超管进店后）  │
│    → 首页：当前门店数据看板 + 待办               │
│    → 运营：当前门店预约/签到记录                 │
│    → 会员：当前门店会员列表                      │
│    → 店务：当前门店运营管理（教练/薪酬/素材等）    │
│    → 我的：门店信息 + 退出登录                   │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 三、登录智能分流

### 3.1 后端改造（auth.service.js → adminLogin）

登录成功返回增加字段：

```javascript
{
  token: '...',
  admin: {
    // ...现有所有字段不变
    login_mode: 'platform' | 'store',
    default_store_id: '...',    // 门店角色自动进入的门店ID
    default_store_name: '...',  // 门店名称
  }
}
```

分流规则：
- `staff` → `login_mode: 'store'`
- `store_manager` → `login_mode: 'store'`
- `super_admin` → `login_mode: 'platform'`
- `reviewer` → `login_mode: 'platform'`

### 3.2 前端改造（login.js）

```javascript
// onLogin 成功回调
const admin = res.data.admin;

// 保存用户信息
app.globalData.userInfo = admin;
app.globalData.token = res.data.token;

if (admin.login_mode === 'store') {
  // 门店角色：设置门店上下文
  app.globalData.spaceMode = 'store';
  app.globalData.currentStoreId = admin.default_store_id;
  app.globalData.currentStore = {
    _id: admin.default_store_id,
    name: admin.default_store_name
  };
} else {
  // 平台角色：默认平台视图
  app.globalData.spaceMode = 'platform';
  app.globalData.currentStoreId = '';
  app.globalData.currentStore = null;
}

wx.switchTab({ url: '/pages/dashboard/dashboard' });
```

---

## 四、全局状态管理（app.js）

### 4.1 新增字段

```javascript
globalData: {
  // ...现有字段不变
  spaceMode: 'platform',      // 'platform' | 'store'
  currentStoreId: '',          // 当前生效的门店ID（空=全平台）
  currentStore: null,          // 当前门店对象
  platformStoreId: '',         // 超管进入门店空间时的临时门店ID（用于返回平台后恢复）
  platformStoreName: '',
}
```

### 4.2 新增方法

```javascript
// 进入门店（超管专用）
enterStore(store) {
  this.globalData.spaceMode = 'store';
  this.globalData.currentStoreId = store._id;
  this.globalData.currentStore = store;
  this.globalData.platformStoreId = store._id;
  this.globalData.platformStoreName = store.name;
  // 触发当前页面刷新
  const pages = getCurrentPages();
  const currentPage = pages[pages.length - 1];
  if (currentPage && typeof currentPage.onSpaceModeChanged === 'function') {
    currentPage.onSpaceModeChanged();
  }
}

// 返回平台（超管专用）
backToPlatform() {
  this.globalData.spaceMode = 'platform';
  this.globalData.currentStoreId = '';
  this.globalData.currentStore = null;
  this.globalData.platformStoreId = '';
  // 触发当前页面刷新
  const pages = getCurrentPages();
  const currentPage = pages[pages.length - 1];
  if (currentPage && typeof currentPage.onSpaceModeChanged === 'function') {
    currentPage.onSpaceModeChanged();
  }
}

// 判断当前是否为门店视图
isStoreMode() {
  return this.globalData.spaceMode === 'store';
}

// 判断当前是否为平台视图
isPlatformMode() {
  return this.globalData.spaceMode === 'platform';
}

// 获取当前有效的store_id（用于API请求参数）
getEffectiveStoreId() {
  if (this.globalData.spaceMode === 'store') {
    return this.globalData.currentStoreId;
  }
  return ''; // 平台模式不传store_id，查全部门店
}
```

---

## 五、超管"进入门店"与"返回平台"

### 5.1 进入门店

超管在平台模式首页看到门店卡片列表，点击"进入门店"：

```javascript
// dashboard.js - 平台模式
onEnterStore(e) {
  const storeId = e.currentTarget.dataset.id;
  const store = this.data.storeList.find(s => String(s._id) === String(storeId));
  if (!store) return;

  // 调用后端接口获取门店上下文token
  request({
    url: '/auth/enter-store',
    method: 'POST',
    data: { store_id: storeId }
  }).then(res => {
    // 更新token（带门店上下文）
    app.globalData.token = res.data.token;
    wx.setStorageSync('admin_token', res.data.token);
    // 切换空间模式
    app.enterStore(store);
    // 刷新当前页面
    this.loadDashboardData();
  });
}
```

### 5.2 返回平台

门店模式下，导航栏显示"返回平台"按钮（仅超管可见）：

```javascript
// 每个页面在onShow时判断
onShow() {
  const isStoreMode = app.isStoreMode();
  const isSuperAdmin = app.globalData.userInfo?.role === 'super_admin';
  
  if (isStoreMode && isSuperAdmin) {
    wx.setNavigationBarTitle({ title: app.globalData.currentStore?.name || '门店' });
    // 通过自定义导航栏或页面内顶部栏显示"返回平台"
    this.setData({ showBackToPlatform: true });
  } else {
    this.setData({ showBackToPlatform: false });
  }
}

// 返回平台
onBackToPlatform() {
  app.backToPlatform();
  this.loadData(); // 刷新为平台视图数据
}
```

### 5.3 门店角色：无任何返回入口

门店角色 `spaceMode` 固定为 `'store'`：
- 导航栏不显示"返回平台"
- 无门店选择器
- 个人中心不显示平台功能入口
- `showBackToPlatform` 始终为 `false`

---

## 六、各页面改造详情

### 6.1 首页（pages/dashboard/dashboard）

**平台模式（超管默认）**：
- 顶部：全平台汇总数据卡片（总门店数、今日总课程、总会员数）
- 中部：门店卡片列表，每个卡片显示：
  - 门店名称
  - 今日课程数
  - 会员数
  - "进入门店"按钮
- 待办事项：全平台汇总，每个待办项展开后**显示门店标签**

**门店模式**：
- 与现有单门店角色首页完全一致
- 本店数据看板、待办事项、近期课程
- 无门店选择器
- 导航栏标题显示门店名称

**改造方式**：
- 在 `onShow` / `loadDashboardData` 开头判断 `app.isStoreMode()`
- 如果是平台模式，调用 `/home/admin` 和 `/stats/dashboard` 时不传 `store_id`（查全平台汇总）
- 新增门店列表请求（已有 `/stores` 接口）
- 两种模式渲染不同的 wxml 区块（用 `wx:if="{{spaceMode === 'platform'}}"` 控制）

#### 6.1.1 平台模式待办事项详细设计

**数据来源**（现有接口，不传store_id即可查全平台）：

| 待办项 | 数据来源 | 已包含门店信息 |
|--------|---------|---------------|
| 会员审核 | `/home/admin` → `pending_review` | 展开后 `/members` 返回的每条记录已 populate `store_id.name` |
| 今日课程 | `/home/admin` → `stats.today_schedules` | 已 populate `store_id.name` |
| 时间卡到期 | `/stats/dashboard` → `expiring_time_cards` | 已包含 `store_name` 字段 |
| 次卡跟进 | `/stats/dashboard` → `count_card_alerts` | 已包含 `store_name` 字段 |
| 近期课程 | `/stats/dashboard` → `upcoming_schedules` | 已包含 `store_name` 字段 |

**展开卡片显示门店标签**：

现有代码中 `_quickMapCard` 和 `_enrichMemberCards` 已提取 `store_name`，`_enrichScheduleCards` 已提取 `store_name`。平台模式下仅需确保：

1. **待办汇总行**：显示"共N条，来自M个门店"（如"时间卡到期 3人，来自2个门店"）
2. **展开的会员卡片**：在卡片右上角或底部显示门店标签（数据已有 `store_name`）
3. **展开的课程卡片**：在卡片中显示门店名称（数据已有 `store_name`）

**点击跳转验证**：

| 操作 | 跳转目标 | 业务逻辑正确性 |
|------|---------|---------------|
| 点击会员卡片 | `/package-member/pages/members/member-detail/member-detail?id=xxx` | `member_id` 全局唯一，后端根据 member_id 查询，与门店无关，正常显示 |
| 点击课程卡片 | `/package-schedule/pages/bookings/bookings?schedule_id=xxx` | `schedule_id` 全局唯一，后端根据 schedule_id 查询预约名单，正常显示 |
| 审核通过会员 | 调用 `/members/{id}/review` | 已有门店选择弹窗，审核时指定门店，正常 |
| 点击"今日课程"统计卡 | `wx.switchTab` 到运营页 | 通过 `app.globalData.pendingStoreId` 传递门店上下文，正常 |

**结论**：现有接口已返回门店信息，现有跳转逻辑基于全局唯一ID（member_id、schedule_id），**平台模式下待办事项的展示和跳转无需额外改造**，仅需在UI上确保门店标签可见。

### 6.2 运营（pages/operations/operations）

**平台模式**：
- 显示全平台预约记录、签到记录
- 显示门店筛选器（可筛选查看特定门店）

**门店模式**：
- 仅显示当前门店数据
- 无门店筛选器
- 与现有单门店角色运营页面一致

**改造方式**：
- 请求时根据 `spaceMode` 决定是否传 `store_id`
- 门店模式下隐藏门店筛选器

### 6.3 会员（pages/members/members）

**平台模式**：
- 全平台会员列表
- 搜索支持跨门店
- 审核员可审核全平台待审核会员

**门店模式**：
- 仅显示当前门店会员
- 与现有单门店角色会员页面一致

**改造方式**：
- 请求 `/members` 时根据 `spaceMode` 决定是否传 `store_id`
- 门店模式下隐藏门店相关筛选

### 6.4 店务（pages/shop/shop）

**平台模式**：
- 显示"系统设置"分类：门店维护、账号管理、全局配置、系统参数
- 显示"运营管理"分类：各项功能入口，但点击时提示"请先进入门店空间"

**门店模式**：
- 与现有单门店角色店务页面完全一致
- 教练管理、薪酬配置、图片素材、公告轮播、放假设置、豁免设置
- 门店信息查看/编辑
- 无门店选择器

**改造方式**：
- `shop.js` 中根据 `app.isStoreMode()` 渲染不同的功能列表
- 平台模式下去掉门店选择器逻辑（已有 `isSingleStoreRole` 判断）

### 6.5 排课（package-schedule/pages/schedule/schedule）

**平台模式**：
- 此页面不直接对平台模式展示排课功能
- 可通过首页门店卡片"进入门店"后跳转过来

**门店模式**：
- 与现有单门店角色排课页面完全一致
- 无门店选择器

**改造方式**：
- 已有 `isSingleStoreRole` 判断逻辑，门店模式下自动注入 `store_id`
- 超管进入门店后，通过 `app.getEffectiveStoreId()` 获取门店ID

### 6.6 个人中心（pages/profile/profile）

**平台模式**：
- 显示平台管理员信息
- 可管理门店列表
- 系统设置入口（账号管理、全局配置等）
- 退出登录

**门店模式**：
- 显示门店管理员信息
- 当前门店名称
- 退出登录（退出后回到登录页）

**改造方式**：
- 根据 `spaceMode` 显示/隐藏平台功能入口

### 6.7 子页面改造原则

所有子页面（排课、预约名单、签到、教练管理、薪酬、素材、放假、豁免、账号管理等）遵循统一原则：

```javascript
// 获取store_id的统一方式
getStoreId() {
  if (app.isStoreMode()) {
    return app.globalData.currentStoreId;
  }
  // 平台模式：从页面自身的门店选择器获取（如果有）
  return this.data.selectedStoreId || '';
}
```

---

## 七、后端改造

### 7.1 新增接口：POST /api/v1/auth/enter-store

```javascript
// auth.routes.js
router.post('/enter-store', auth, async (req, res) => {
  // 仅超管可调用
  if (req.user.role !== 'super_admin') {
    return res.status(403).json({ code: 403, message: '仅超级管理员可进入门店空间' });
  }
  
  const { store_id } = req.body;
  const Store = require('../models/Store');
  const store = await Store.findById(store_id);
  if (!store) return res.status(404).json({ code: 404, message: '门店不存在' });
  
  const jwt = require('jsonwebtoken');
  const config = require('../config');
  
  // 签发带门店上下文的临时token（12小时有效）
  const token = jwt.sign({
    id: req.user.id,
    role: req.user.role,
    user_type: req.user.user_type,
    permissions: req.user.permissions,
    impersonate_store_id: String(store_id),
    impersonate_store_name: store.name,
  }, config.jwtSecret, { expiresIn: '12h' });
  
  res.json({ code: 200, message: 'success', data: { token, store } });
});
```

### 7.2 auth 中间件解析 impersonate_store_id

```javascript
// auth.js - 在现有解析逻辑中增加
req.user = {
  ...decoded,
  id: String(user._id),
  role: user.role,
  // ...现有字段
  impersonate_store_id: decoded.impersonate_store_id || null,
  impersonate_store_name: decoded.impersonate_store_name || null,
};
```

### 7.3 storeFilter 中间件增强

```javascript
// storeFilter.js - 在现有逻辑之前增加
if (req.user.impersonate_store_id) {
  // 超管"进入门店"模式：强制所有操作限定在该门店
  req.storeFilter = { store_id: req.user.impersonate_store_id };
  if (req.method === 'GET') {
    req.query.store_id = req.user.impersonate_store_id;
  }
  if (req.method === 'POST') {
    req.body.store_id = req.user.impersonate_store_id;
  }
  return next(); // 跳过后续角色判断，直接使用注入的store_id
}
```

### 7.4 adminLogin 返回增加字段

```javascript
// auth.service.js - adminLogin 函数
const admin = user.toObject();
delete admin.password;

// 计算 login_mode
let loginMode = 'platform';
let defaultStoreId = '';
let defaultStoreName = '';

if (user.role === 'staff') {
  loginMode = 'store';
  defaultStoreId = String(user.store_id);
} else if (user.role === 'store_manager') {
  loginMode = 'store';
  defaultStoreId = String(user.store_ids[0]);
}

// 查门店名称
if (defaultStoreId) {
  const store = await Store.findById(defaultStoreId).select('name').lean();
  defaultStoreName = store?.name || '';
}

return {
  token,
  admin: {
    ...admin,
    login_mode: loginMode,
    default_store_id: defaultStoreId,
    default_store_name: defaultStoreName,
  }
};
```

### 7.5 操作日志增加门店上下文

```javascript
// log.service.js 或相关日志记录处
const logEntry = {
  // ...现有字段
};

if (req.user.impersonate_store_id) {
  logEntry.operated_as = 'platform_admin_in_store';
  logEntry.target_store_id = req.user.impersonate_store_id;
}
```

---

## 八、不需要改动的部分

| 模块 | 原因 |
|------|------|
| 教练数据模型 | 保持多门店执教，门店独立核算薪酬 |
| 跨门店套餐 | 保持 extra_store_ids 机制 |
| 会员数据模型 | 保持 store_id + 跨门店套餐逻辑 |
| 预约/签到流程 | 保持现有 storeFilter 过滤 |
| 薪酬计算 | 保持现有按门店独立计算 |
| 图片素材 | 保持现有门店归属 + 公共素材 |
| 消息推送 | 保持现有按门店独立发送 |
| WebSocket | 保持现有在线状态推送 |
| 定时任务 | 保持现有按门店分片执行 |
| 所有后端路由 | 保持现有路径和参数结构 |

---

## 九、实施阶段

### 阶段一：后端改造（1天）

1. 新增 `POST /api/v1/auth/enter-store` 接口
2. `auth` 中间件解析 `impersonate_store_id`
3. `storeFilter` 中间件增加 `impersonate_store_id` 强制注入
4. `adminLogin` 返回增加 `login_mode`、`default_store_id`、`default_store_name`
5. 操作日志增加门店上下文记录

### 阶段二：前端全局状态 + 登录分流（1天）

1. `app.js` 新增 `spaceMode`、`currentStoreId`、`platformStoreId` 及辅助方法
2. `login.js` 登录成功后按 `login_mode` 分流
3. 封装 `getEffectiveStoreId()` 供所有页面使用

### 阶段三：首页改造（1天）

1. **平台模式首页**：全平台汇总数据 + 门店卡片列表 + "进入门店"按钮
2. **门店模式首页**：与现有单门店首页一致
3. 门店模式下超管可见"返回平台"导航

### 阶段四：运营/会员/店务页面适配（1.5天）

1. **运营页**：平台模式传store_id=空，门店模式传currentStoreId
2. **会员页**：同上
3. **店务页**：平台模式显示系统设置，门店模式显示运营管理
4. 各页面门店模式下隐藏门店选择器

### 阶段五：子页面适配 + 测试（1.5天）

1. 排课、预约名单、签到、教练、薪酬、素材、放假、豁免等子页面适配
2. 账号管理：门店模式仅管理本店店员
3. 门店维护：门店模式仅查看/编辑本店
4. 全角色、全场景功能测试
5. 越权测试

---

## 十、关键设计决策

| 决策 | 说明 |
|------|------|
| 不改TabBar | 保持 首页/运营/会员/店务/我的 五个Tab不变，页面内部自适应 |
| 不拆API路由 | 保持现有 `/api/v1/*` 路由体系，通过 storeFilter 中间件隔离 |
| 不拆账号表 | 保持现有 User 模型，role 字段区分角色 |
| 超管进入门店换token | 签发带 `impersonate_store_id` 的临时token，后端强制注入store_id |
| 门店角色不动 | 现有单门店角色体验完全不变，无需任何改造 |
| 页面内部判断 | 每个页面在onShow时读取 `app.isStoreMode()` 决定渲染逻辑 |

---

## 十一、总结

本方案的核心思路是：**在现有系统基础上，通过页面内部自适应实现"两个空间"的体验，不改变TabBar、不改变API路由、不改变数据模型。**

- 门店角色登录后，体验与现在完全一致（已有单门店隔离）
- 超管登录后默认看到平台视图，可点击门店卡片"进入门店"切换到门店视图
- 门店视图下，超管的操作权限与门店店长一致，所有数据限定在该门店
- 通过后端 `impersonate_store_id` 强制注入，确保超管在门店模式下无法越权访问其他门店数据